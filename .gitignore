# Node.js dependencies (ignore node_modules for both client and server)
node_modules/
Server/node_modules/
client/node_modules/

# React build output (frontend build directory)
client/build/

# Server build output (if using build step for server, like Babel or TypeScript)
server/dist/

# Logs (ignore log files)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (ignore .env files for both client and server)
client/.env
server/.env

# OS-generated files (ignore OS-specific hidden files)
.DS_Store   # macOS
Thumbs.db   # Windows

# IDE files (ignore IDE config files)
.vscode/
.idea/
*.swp

# Temporary files and backup files
*.bak
*.tmp

# Miscellaneous
coverage/   # For code coverage results

# Docker files (optional, if using Docker)
.dockerignore
